<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>

    <link rel="stylesheet" href="animate.min.css">
    <!-- <link rel="stylesheet" type="text/css" media="screen and (<!-- max-device-width: 480px)" href="app/assets/stylesheets/mobile.css"> --> -->
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="canonical" href="https://www.wegscheidwebservices.com">
    <link rel="alternate" media="only screen and (max-width: 640px)" href="https://m.wegscheidwebservices.com"> -->
    <link href="https://fonts.googleapis.com/css?family=Raleway:100,200,300&display=swap" rel="stylesheet">
  </head>

  <body>
    <div id="container" class="container">
      <%= yield %>
      <%= render 'layouts/header' %>
      <%= render 'layouts/footer' %>
<!--       <%= debug(params) if Rails.env.development? %> -->
    </div>
  </body>
</html>
